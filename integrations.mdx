---
title: Integrations and Extensions
description: Connect Annelise Lawson O'BAILEY with third-party tools to enhance your documentation workflow.
---

## Overview

Extend Annelise Lawson O'BAILEY by integrating with external services. You can set up webhooks for real-time notifications, use the API for custom automation, connect popular tools like Slack and GitHub, and embed content from other platforms. These integrations streamline your documentation processes, from automated updates to cross-platform publishing.

<Callout kind="info">
  All integrations require an API key from your dashboard at `https://dashboard.example.com/settings/api`.
</Callout>

## Popular Third-Party Connections

Discover ready-to-use integrations that supercharge your workflow.

<Columns cols={3}>
  <Card title="Slack Notifications" icon="message-circle" href="https://slack.com/apps" target="_blank">
    Receive doc updates and review alerts in your Slack channels.
  </Card>
  <Card title="GitHub Sync" icon="github" href="https://github.com/integrations" target="_blank">
    Automatically sync documentation changes with your repos.
  </Card>
  <Card title="Zapier" icon="zap" href="https://zapier.com/apps/annelise-lawson-obailey" target="_blank">
    Build no-code automations with 5000+ apps.
  </Card>
</Columns>

## Setting Up Webhooks for Notifications

Webhooks notify your services about events like doc updates or publishes. Configure them in your dashboard under Settings > Webhooks.

<Steps>
  <Step title="Create Webhook" icon="plus">
    Navigate to `https://dashboard.example.com/settings/webhooks` and click "New Webhook".
  </Step>
  <Step title="Add Endpoint URL" icon="link">
    Enter your endpoint, like `https://your-webhook-url.com/annelise`.
  </Step>
  <Step title="Select Events" icon="bell">
    Choose events such as "Document Updated" or "Page Published".
  </Step>
  <Step title="Verify and Save" icon="check-circle">
    Test the webhook and save. Monitor deliveries in the dashboard.
  </Step>
</Steps>

Handle incoming webhooks securely. Verify signatures using your secret.

<CodeGroup tabs="JavaScript,Python">
  ```javascript
  const crypto = require('crypto');

  app.post('/annelise-webhook', (req, res) => {
    const signature = req.headers['x-annelise-signature'];
    const payload = JSON.stringify(req.body);
    const computed = 'sha256=' + crypto.createHmac('sha256', process.env.WEBHOOK_SECRET)
      .update(payload).digest('hex');

    if (crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(computed))) {
      console.log('Event:', req.body.event);
      res.status(200).send('OK');
    } else {
      res.status(401).send('Unauthorized');
    }
  });
  ```
  ```python
  import hmac
  import hashlib
  from flask import Flask, request, abort

  app = Flask(__name__)
  WEBHOOK_SECRET = 'your-webhook-secret-here'

  @app.route('/annelise-webhook', methods=['POST'])
  def webhook():
      signature = request.headers.get('X-Annelise-Signature')
      payload = request.get_data()
      computed = 'sha256=' + hmac.new(
          WEBHOOK_SECRET.encode(), payload, hashlib.sha256
      ).hexdigest()

      if hmac.compare_digest(signature, computed):
          print('Event:', request.json['event'])
          return 'OK', 200
      else:
          abort(401)
  ```
</CodeGroup>

## API Access for Custom Integrations

Access Annelise Lawson O'BAILEY's API at `https://api.example.com/v1` for programmatic control. Authenticate requests with Bearer tokens.

<ParamField header="Authorization" param-type="string" required="true">
  Bearer `{YOUR_API_KEY}`.
</ParamField>

<ParamField query="project_id" param-type="string" required="true">
  Your project identifier from the dashboard.
</ParamField>

Example: Fetch recent documents.

<Request tabs="cURL,JavaScript">
  ```bash
  curl -X GET "https://api.example.com/v1/documents?project_id=YOUR_PROJECT_ID" \
    -H "Authorization: Bearer YOUR_API_KEY"
  ```
  ```javascript
  const response = await fetch('https://api.example.com/v1/documents?project_id=YOUR_PROJECT_ID', {
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY'
    }
  });
  const docs = await response.json();
  ```
</Request>

<Response tabs="200">
  ```json
  {
    "documents": [
      {
        "id": "doc_123",
        "title": "API Guide",
        "updated_at": "2024-10-15T10:00:00Z"
      }
    ]
  }
  ```
</Response>

## Embedding External Content

Embed tools like Figma designs or Google Forms directly into your docs.

<Tabs>
  <Tab title="Iframe Embed" icon="layout">
    Use secure iframes for interactive content.

    ```
    <Iframe
      src="https://www.figma.com/embed?embed_host=share&url=https://www.figma.com/file/..."
      title="Figma Design"
      width="100%"
      height="600"
    />
    ```

    <Callout kind="tip">
      Always set `title` for accessibility and use `width="100%"` for responsive embeds.
    </Callout>
  </Tab>
  <Tab title="Custom Scripts" icon="code">
    Load external JavaScript safely.

    <CodeGroup tabs="HTML Snippet">
      ```html
      <script src="https://example.com/widget.js" 
              data-project="annelise-lawson-obailey"></script>
      ```
    </CodeGroup>
  </Tab>
</Tabs>

<Callout kind="alert">
  Review embedded content for security. Avoid untrusted sources to prevent XSS risks.
</Callout>